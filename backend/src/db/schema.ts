/**
 * Kysely table types. Tables are added here as migrations create them
 * (M1 onwards). Keep in sync with src/db/migrations/.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Database {}
